#include <stdio.h>

/**
 * main - Entry point
 * Return: 0
 */

int main(void)
{
	int n = '0';
	int hexa = 'a';

	while (c <= '9')
	{
		putchar(c);
		c++;
	}

	while (hexa <= 'f')
	{
		putchar(hexa);
		hexa++;
	}

	putchar('\n');
	return (0);
}
